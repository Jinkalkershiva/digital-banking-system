spring:
  application:
    name: payment-service
  datasource:
    url: jdbc:mysql://localhost:3306/payment_db?createDatabaseIfNotExist=true
    username: root
    password: shivaJ@123
    driver-class-name:
    jpa:
      hibernate:
        ddl-auto: update
      show:sql: true
      properties:
        hibernate:
          dialect: org.hibernate.dialect.MySQLDialect

  kafka:
    bootstrap-server: localhost:9092
    producer:
      key-deserializer: org.apache.kafka.common.serialization.StringSerializer
      value-deserialized: org.springframework.kafka.support.serializer.JsonSerializer

server:
  port: 8083

#Razorpay configuration
razorpay:
  key-id: rzp_xxxxxx
  key-secret:
  webhook-secret:

management:
  endpoints:
    web:
      exposure:
        include: health ,info




